import React from 'react'

function Logo({width= '100px'}) { // passing properties as props in order to adjust the width according to the usage. By default, it is 100px
  return (
    <div>
      Logo
    </div>
  )
}

export default Logo
